spring:
  application:
    name: mcs-notifications
  cloud:
    config:
      uri: http://config-server:8888
      fail-fast: true
      retry:
        max-attempts: 20
        max-interval: 5000
        initial-interval: 1000