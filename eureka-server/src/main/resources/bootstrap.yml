spring:
  application:
    name: eureka-server
  cloud:
    config:
      import: optional:configserver:http://config-server:8888 # Usar el nombre del servicio de Docker
      fail-fast: true # Si el Config Server está disponible pero no encuentra la configuración, falla rápido.
